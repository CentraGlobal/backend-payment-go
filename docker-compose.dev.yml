services:
  payment-service:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    networks:
      - centra-network

networks:
  centra-network:
    name: centra-backend-api-nodejs_default
    external: true
